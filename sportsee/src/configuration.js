const configuration = {
  API: 'http://localhost:3000', // default: 'http://localhost:3000'
  mockedData: false, // ( true: data are mocked | false: get data form api ) if you change the param you need refresh page
}
export default configuration
